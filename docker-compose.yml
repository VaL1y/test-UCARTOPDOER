version: "3.9"

services:
  tests:
    build: .
    command: pytest

  api:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    depends_on:
      tests:
        condition: service_completed_successfully
